---
deployment:
  tasks:
    - source /home/deployment/environment.rc
    - npm i
    - npm run build
    - rm -rf $DEPLOYPATH
    - mkdir $DEPLOYPATH
    - mv build $DEPLOYPATH
    - cp src/server $APIPATH
